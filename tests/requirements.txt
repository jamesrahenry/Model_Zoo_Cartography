# Dependencies to run this test suite (pip install -r tests/requirements.txt).
# CPU only — no GPU needed for any test here (see tests/README.md).
# torch CPU build is fine and much smaller: install it first with
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
# before this file, so the plain `torch` requirement below is already
# satisfied and pip won't re-resolve it from the default (CUDA) index.
-r ../requirements.txt
pytest
